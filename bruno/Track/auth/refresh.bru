meta {
  name: refresh
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}auth/token/
  body: json
  auth: none
}

body:json {
  {
      "username": "{{USERNAME}}",
      "password": "{{PASSWORD}}"
  }
}

vars:post-response {
  jwtAccessToken: res.body.access
  jwtRefreshToken: res.body.refresh
}
