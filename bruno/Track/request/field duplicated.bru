meta {
  name: field duplicated
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}genres/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "name": "genre", "name":"sfds"
  }
}

vars:post-response {
  genreAUuid: res.body.uuid
}
