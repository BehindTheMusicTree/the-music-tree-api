meta {
  name: tracks--uuid--
  type: http
  seq: 1
}

delete {
  url: {{BASE_URL}}tracks/{{trackUuid}}/
  body: json
  auth: bearer
}

headers {
  Authorization: {{authorization}}
}

auth:bearer {
  token: {{tokenAccess}}
}
