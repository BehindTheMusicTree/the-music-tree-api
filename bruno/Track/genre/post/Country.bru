meta {
  name: Country
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}genres/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "name": "Country"
  }
}

vars:post-response {
  genreCountryUuid: res.body.uuid
}
