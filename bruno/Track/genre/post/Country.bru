meta {
  name: Country
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}genres/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "name": "Countrys"
  }
}

vars:post-response {
  genreCountryUuid: res.body.uuid
}
