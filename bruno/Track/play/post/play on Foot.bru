meta {
  name: play on Foot
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}plays/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "contentObjectUuid": "{{genreCountryPlaylistUuid}}"
  }
}
