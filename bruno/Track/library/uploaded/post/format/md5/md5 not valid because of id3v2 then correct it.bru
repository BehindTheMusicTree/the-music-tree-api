meta {
  name: md5 not valid because of id3v2 then correct it
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}library/uploaded/
  body: multipartForm
  auth: inherit
}

headers {
  : 
}

body:multipart-form {
  file: @file(api/test/utils/uploaded_track/files/format=md5 not valid because of id3v2 metadata.flac)
}
