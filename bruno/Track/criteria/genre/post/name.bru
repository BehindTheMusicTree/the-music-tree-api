meta {
  name: name
  type: http
  seq: 7
}

post {
  url: {{BASE_URL}}genres/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "name": "punk"
  }
}

vars:post-response {
  genreAUuid: res.body.uuid
}
