meta {
  name: name duplicated
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}genres/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{tokenAccess}}
}

body:json {
  {
    "name": "genre"
  }
}

vars:post-response {
  genreAUuid: res.body.uuid
}
