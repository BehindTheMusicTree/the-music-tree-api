meta {
  name: Country
  type: http
  seq: 1
}

delete {
  url: {{BASE_URL}}genres/{{genreCountryUuid}}/
  body: json
  auth: inherit
}

body:json {
  {
    "parent": "{{genreCowboyCountryUuid}}"
  }
}
