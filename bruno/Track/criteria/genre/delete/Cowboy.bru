meta {
  name: Cowboy
  type: http
  seq: 2
}

delete {
  url: {{BASE_URL}}genres/{{genreCowboyUuid}}/
  body: json
  auth: inherit
}

body:json {
  {
    "parent": "{{genreCowboyCountryUuid}}"
  }
}
