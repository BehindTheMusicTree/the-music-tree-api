# Gitignored files/directories to copy to new worktrees
# See git-worktree-scripts documentation for format details

# Copy Python virtual environment from env directory
env/.venv

# Copy fixture files directly in api/fixtures
api/fixtures/*.json

